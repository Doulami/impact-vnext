# feature-flags SDK\n\n**Purpose**: Shared SDK to read feature flags from Strapi StoreConfig.\n\n**Status**: Placeholder (M2 will implement)\n\n## Usage\n\n```typescript\nimport { getStoreConfig, FeatureFlags } from '@impact/feature-flags';\n\nconst flags: FeatureFlags = await getStoreConfig();\n\nif (flags.features.payments.gpgEnabled) {\n  // Load GPG payment handler\n}\n\nif (flags.features.loyalty) {\n  // Calculate loyalty points\n}\n```\n\n## Caching\n\n- **API**: 5–10 min TTL (Redis or in-memory)\n- **Browser**: 30 min TTL (localStorage)\n- **Force refresh**: manual cache invalidation endpoint\n\n## Interfaces\n\n```typescript\ninterface FeatureFlags {\n  features: {\n    payments: { gpgEnabled: boolean };\n    shipping: { fileBridgeEnabled: boolean };\n    promotions: boolean;\n    bundles: boolean;\n    loyalty: boolean;\n    search: { meiliEnabled: boolean };\n    analytics: { enabled: boolean };\n    pwa: { enabled: boolean };\n    cache: { edgeEnabled: boolean };\n  };\n  branding: {\n    siteName: string;\n    primaryColor: string;\n    logoUrl: string;\n    socialLinks: Array<{ platform: string; url: string }>;\n  };\n  search?: {\n    meiliHost: string;\n    meiliIndexPrefix: string;\n  };\n}\n```\n\n## Kill Switch\n\nDisable entire SDK: set `FEATURE_FLAGS_ENABLED=false` (fallback to hardcoded defaults).\n"