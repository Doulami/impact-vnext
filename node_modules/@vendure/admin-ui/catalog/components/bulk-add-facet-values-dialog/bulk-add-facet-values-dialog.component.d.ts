import { ChangeDetectorRef, OnDestroy, OnInit } from '@angular/core';
import { DataService, Dialog, FacetValueFragment } from '@vendure/admin-ui/core';
import { Subscription } from 'rxjs';
import * as i0 from "@angular/core";
interface ProductOrVariant {
    id: string;
    name: string;
    sku?: string;
    facetValues: Array<{
        id: string;
        name: string;
        code: string;
        facet: Array<{
            id: string;
            name: string;
            code: string;
        }>;
    }>;
}
export declare class BulkAddFacetValuesDialogComponent implements OnInit, OnDestroy, Dialog<FacetValueFragment[]> {
    private dataService;
    private changeDetectorRef;
    resolveWith: (result?: FacetValueFragment[]) => void;
    mode: 'product' | 'variant';
    ids?: string[];
    state: 'loading' | 'ready' | 'saving';
    selectedValues: FacetValueFragment[];
    items: ProductOrVariant[];
    facetValuesRemoved: boolean;
    private subscription;
    constructor(dataService: DataService, changeDetectorRef: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnDestroy(): void;
    cancel(): void;
    removeFacetValue(item: ProductOrVariant, facetValueId: string): void;
    addFacetValues(): Subscription;
    static ɵfac: i0.ɵɵFactoryDeclaration<BulkAddFacetValuesDialogComponent, never>;
    static ɵcmp: i0.ɵɵComponentDeclaration<BulkAddFacetValuesDialogComponent, "vdr-bulk-add-facet-values-dialog", never, {}, {}, never, never, false, never>;
}
export {};
